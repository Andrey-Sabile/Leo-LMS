import { Routes } from '@angular/router';
import { StudentDirectoryComponent } from './components/student-directory/student-directory.component';

export const STUDENT_DIRECTORY_ROUTES: Routes = [
  {
    path: 'student-directory',
    component: StudentDirectoryComponent
  }
];
